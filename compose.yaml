services:
  mex-editor:
    build:
      context: .
    ports:
      - 8081:8081
    environment:
      - MEX_EDITOR_HOST=0.0.0.0
      - MEX_EDITOR_PORT=8080
    expose:
      - 8081
    healthcheck:
      test: [ "CMD", "curl", "http://0.0.0.0:8080/_system/check" ]
      interval: 60s
      timeout: 5s
      retries: 5
    volumes:
      - data:/app/data
volumes:
  data:
